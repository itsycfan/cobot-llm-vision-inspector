# docs/

- `assembly_instructions_v2.xlsx`: originally named "AssemblyInstructions - 准确率90的时候用的版本.xlsx" ("the version used when accuracy was 90%"). This is the assembly-instruction version used in the 2025-10-28/29 experiments that reached ~90% accuracy, and matches the scripts kept in this repository.
- There is also a slightly different `AssemblyInstructions.xlsx` (unlabeled version). An md5 check confirmed the two files are not identical, so to avoid version confusion the unlabeled one was kept only as a local backup (`cobot-llm-vision-inspector-release-assets/assembly_instructions_v1_original.xlsx`) and was not uploaded to the repository. Let me know if you'd like it archived here too.
- `gpt_vs_claude_comparison.docx`: comparison report of GPT-4V vs. Claude visual-detection performance.
